# openauth-stripe

Alias package for `better_auth-stripe`.

## Installation

```ruby
gem "openauth-stripe"
```

```ruby
require "openauth/stripe"
```

This package depends on `better_auth-stripe` and loads the canonical Better Auth Ruby Stripe billing plugin.

For documentation, visit https://better-auth-rb.vercel.app/.
