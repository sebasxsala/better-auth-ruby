# openauth-rails

Alias package for `better_auth-rails`.

## Installation

```ruby
gem "openauth-rails"
```

```ruby
require "openauth/rails"
```

This package depends on `better_auth-rails` and loads the canonical Better Auth Ruby Rails integration.

For documentation, visit https://better-auth-rb.vercel.app/.
