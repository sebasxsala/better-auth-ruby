# openauth-api-key

Alias package for `better_auth-api-key`.

## Installation

```ruby
gem "openauth-api-key"
```

```ruby
require "openauth/api_key"
```

This package depends on `better_auth-api-key` and loads the canonical Better Auth Ruby API key plugin.

For documentation, visit https://better-auth-rb.vercel.app/.
