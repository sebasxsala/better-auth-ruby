# openauth-scim

Alias package for `better_auth-scim`.

## Installation

```ruby
gem "openauth-scim"
```

```ruby
require "openauth/scim"
```

This package depends on `better_auth-scim` and loads the canonical Better Auth Ruby SCIM provisioning plugin.

For documentation, visit https://better-auth-rb.vercel.app/.
