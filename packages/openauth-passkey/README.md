# openauth-passkey

Alias package for `better_auth-passkey`.

## Installation

```ruby
gem "openauth-passkey"
```

```ruby
require "openauth/passkey"
```

This package depends on `better_auth-passkey` and loads the canonical Better Auth Ruby Passkey/WebAuthn plugin.

For documentation, visit https://better-auth-rb.vercel.app/.
